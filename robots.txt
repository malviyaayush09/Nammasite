# Cloudflare injects its managed robots.txt — the content signals and the
# AI-crawler blocks (Amazonbot, CCBot, ClaudeBot, GPTBot, Google-Extended,
# meta-externalagent and friends) are appended to this file automatically.
#
# Do NOT restate those rules here. Cloudflare appends rather than replaces,
# so a full copy produced every group twice, including two `User-agent: *`
# blocks — harmless to Google, which merges them, but ambiguous per spec
# and some crawlers honour only the first.
#
# The one thing the managed file never had is a Sitemap line, so that is
# all this file contributes.

Sitemap: https://nammasite.co.in/sitemap.xml
